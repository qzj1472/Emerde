<UserControl x:Class="Emerde.Controls.UiXNotificationHost"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:core="clr-namespace:Emerde.Core"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             HorizontalContentAlignment="Stretch"
             VerticalContentAlignment="Top"
             Focusable="False"
             FocusVisualStyle="{x:Null}"
             IsTabStop="False"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True">
    <UserControl.Resources>
        <Style x:Key="UiXNotificationIconButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Width" Value="32" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource UiXTextSecondaryBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource UiXSubtleFillBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource UiXTextPrimaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.72" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="UiXNotificationActionButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="MinWidth" Value="72" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="Background" Value="{DynamicResource UiXSelectionFillBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource UiXSelectionStrokeBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource UiXTextPrimaryBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Chrome"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource UiXDialogSelectionBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.75" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Background="{x:Null}" IsHitTestVisible="True">
        <ItemsControl x:Name="NotificationItems"
                      Width="480"
                      MaxWidth="520"
                      Margin="16,12"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Top"
                      ItemsSource="{Binding VisibleNotifications, ElementName=Root}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type core:AppFeedbackNotification}">
                    <Border x:Name="NotificationCard"
                            Margin="0,0,0,8"
                            Padding="16,14"
                            Background="{DynamicResource UiXDialogElevatedBrush}"
                            BorderBrush="{DynamicResource UiXStrongStrokeBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            MouseEnter="NotificationCardMouseEnter"
                            MouseLeave="NotificationCardMouseLeave"
                            PreviewMouseLeftButtonDown="NotificationCardMouseLeftButtonDown"
                            PreviewMouseMove="NotificationCardMouseMove"
                            PreviewMouseLeftButtonUp="NotificationCardMouseLeftButtonUp"
                            LostMouseCapture="NotificationCardLostMouseCapture"
                            Loaded="NotificationCardLoaded"
                            AutomationProperties.Name="{Binding Title}"
                            SnapsToDevicePixels="True">
                        <Border.RenderTransform>
                            <TranslateTransform />
                        </Border.RenderTransform>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid Margin="0,0,8,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Ellipse x:Name="SemanticIndicator"
                                             Width="8"
                                             Height="8"
                                             Margin="0,1,9,0"
                                             VerticalAlignment="Center"
                                             Fill="{DynamicResource SystemAccentColorPrimaryBrush}" />
                                    <TextBlock Grid.Column="1"
                                               FontSize="15"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource UiXTextPrimaryBrush}"
                                               Text="{Binding Title}"
                                               TextWrapping="Wrap" />
                                </Grid>
                                <ScrollViewer Grid.Row="1"
                                              Margin="17,5,0,0"
                                              MaxHeight="220"
                                              HorizontalScrollBarVisibility="Disabled"
                                              VerticalScrollBarVisibility="Auto">
                                    <ScrollViewer.Style>
                                        <Style TargetType="{x:Type ScrollViewer}">
                                            <Setter Property="Visibility" Value="Visible" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasBody}" Value="False">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ScrollViewer.Style>
                                    <TextBlock FontSize="13"
                                               Foreground="{DynamicResource UiXTextSecondaryBrush}"
                                               LineHeight="21"
                                               Text="{Binding Body}"
                                               TextWrapping="Wrap" />
                                </ScrollViewer>
                                <ProgressBar Grid.Row="2"
                                              Height="3"
                                              Margin="17,11,0,0"
                                              IsIndeterminate="{Binding IsIndeterminate, Mode=OneWay}"
                                              Maximum="100"
                                              Value="{Binding ProgressValue, Mode=OneWay}">
                                    <ProgressBar.Style>
                                        <Style TargetType="{x:Type ProgressBar}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsTask}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ProgressBar.Style>
                                </ProgressBar>
                                <StackPanel Grid.Row="3" Margin="17,11,0,0" HorizontalAlignment="Left">
                                    <Button CommandParameter="{Binding Id}"
                                            Content="{Binding ActionLabel}"
                                            Click="ActionButtonClick">
                                        <Button.Style>
                                            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource UiXNotificationActionButtonStyle}">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasAction}" Value="False">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsActionRunning}" Value="True">
                                                        <Setter Property="IsEnabled" Value="False" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </Grid>
                            <Button Grid.Column="1"
                                    Margin="0,-5,-5,0"
                                    VerticalAlignment="Top"
                                    CommandParameter="{Binding Id}"
                                    Click="DismissButtonClick"
                                    Style="{StaticResource UiXNotificationIconButtonStyle}"
                                    ToolTip="{I18N ButtonOfClose}">
                                <ui:SymbolIcon FontSize="16" Symbol="Dismiss20" />
                            </Button>
                        </Grid>
                    </Border>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding Kind}" Value="{x:Static core:AppFeedbackKind.Success}">
                            <Setter TargetName="SemanticIndicator" Property="Fill" Value="{DynamicResource UiXLiveFillBrush}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Kind}" Value="{x:Static core:AppFeedbackKind.Information}">
                            <Setter TargetName="SemanticIndicator" Property="Fill" Value="{DynamicResource UiXStallForegroundBrush}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Kind}" Value="{x:Static core:AppFeedbackKind.Warning}">
                            <Setter TargetName="SemanticIndicator" Property="Fill" Value="{DynamicResource UiXTranscodeForegroundBrush}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Kind}" Value="{x:Static core:AppFeedbackKind.Error}">
                            <Setter TargetName="SemanticIndicator" Property="Fill" Value="{DynamicResource UiXDangerForegroundBrush}" />
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
