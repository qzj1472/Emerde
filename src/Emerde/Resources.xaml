<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Emerde.Controls"
                    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                    xmlns:vio="clr-namespace:Wpf.Ui.Violeta.Controls;assembly=Wpf.Ui.Violeta">

    <FontFamily x:Key="VioletaFluentIcons">pack://application:,,,/Wpf.Ui.Violeta;component/Resources/Fonts/Violeta Fluent Icons.ttf#Violeta Fluent Icons</FontFamily>

    <CornerRadius x:Key="Win11ControlCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="RoundCheckBoxCornerRadius">10</CornerRadius>
    <CornerRadius x:Key="ControlCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="OverlayCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="TextControlCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="DefaultControlCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="DefaultOverlayCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="DefaultTextControlCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="ControlCornerRadiusLarge">8</CornerRadius>
    <CornerRadius x:Key="CardCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="SettingsCardCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="ButtonCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="ComboBoxCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="TextBoxCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="ContentDialogCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="PopupCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="FlyoutCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="MenuFlyoutItemCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="ToggleSwitchCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="CheckBoxCornerRadius">10</CornerRadius>
    <SolidColorBrush x:Key="EmerdeShellBackgroundBrush" Color="#F3F3F3" />
    <SolidColorBrush x:Key="EmerdeSurfaceBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="EmerdePanelBrush" Color="#F8F8F8" />
    <SolidColorBrush x:Key="EmerdeCardBrush" Color="#FFFFFF" />
    <StaticResource x:Key="EmerdeTextInputBorderBrush" ResourceKey="ControlStrokeColorDefaultBrush" />
    <SolidColorBrush x:Key="ControlElevationBorderBrush" Color="#24000000" />
    <SolidColorBrush x:Key="CircleElevationBorderBrush" Color="#24000000" />
    <SolidColorBrush x:Key="AccentControlElevationBorderBrush" Color="Transparent" />
    <SolidColorBrush x:Key="DialogMaskLightBrush" Color="#33FFFFFF" />
    <SolidColorBrush x:Key="DialogMaskDarkBrush" Color="#33000000" />
    <StaticResource x:Key="DialogMaskBrush" ResourceKey="DialogMaskLightBrush" />
    <StaticResource x:Key="ContentDialogSmokeFill" ResourceKey="DialogMaskBrush" />
    <StaticResource x:Key="ContentDialogLightDismissOverlayBackground" ResourceKey="DialogMaskBrush" />
    <LinearGradientBrush x:Key="VerticalEdgeFadeMask" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Offset="0" Color="#00FFFFFF" />
        <GradientStop Offset="0.025" Color="#FFFFFFFF" />
        <GradientStop Offset="0.975" Color="#FFFFFFFF" />
        <GradientStop Offset="1" Color="#00FFFFFF" />
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="TopEdgeFadeOpacityMask" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Offset="0" Color="#FFFFFFFF" />
        <GradientStop Offset="1" Color="#00FFFFFF" />
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="BottomEdgeFadeOpacityMask" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Offset="0" Color="#00FFFFFF" />
        <GradientStop Offset="1" Color="#FFFFFFFF" />
    </LinearGradientBrush>

    <Style TargetType="{x:Type ToolTip}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="controls:MotionAssist.IsEntranceEnabled" Value="True" />
        <Setter Property="controls:MotionAssist.EntranceOffsetY" Value="4" />
        <Setter Property="controls:MotionAssist.EntranceScale" Value="0.98" />
        <Setter Property="HasDropShadow" Value="True" />
        <Setter Property="Padding" Value="10,7" />
        <Setter Property="MaxWidth" Value="360" />
        <Setter Property="Background" Value="{DynamicResource EmerdeSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToolTip}">
                    <Border Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource Win11ControlCornerRadius}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Content="{TemplateBinding Content}"
                                          ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          RecognizesAccessKey="True" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="controls:MotionAssist.IsPressEnabled" Value="True" />
        <Setter Property="MinHeight" Value="34" />
    </Style>

    <Style TargetType="{x:Type ToggleButton}" BasedOn="{StaticResource {x:Type ToggleButton}}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="controls:MotionAssist.IsPressEnabled" Value="True" />
        <Setter Property="MinHeight" Value="34" />
    </Style>

    <Style TargetType="{x:Type CheckBox}" BasedOn="{StaticResource {x:Type CheckBox}}">
        <Setter Property="controls:MotionAssist.IsPressEnabled" Value="True" />
        <Setter Property="MinHeight" Value="24" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <Grid MinHeight="{TemplateBinding MinHeight}"
                          Background="Transparent"
                          SnapsToDevicePixels="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border x:Name="CheckBoxGlyphHost"
                                Width="20"
                                Height="20"
                                VerticalAlignment="Center"
                                Background="{DynamicResource ControlFillColorDefaultBrush}"
                                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource RoundCheckBoxCornerRadius}">
                            <Path x:Name="CheckGlyph"
                                  Width="11"
                                  Height="8"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Data="M 1 4 L 4 7 L 10 1"
                                  Stretch="Fill"
                                  Stroke="White"
                                  StrokeEndLineCap="Round"
                                  StrokeStartLineCap="Round"
                                  StrokeThickness="2"
                                  Visibility="Collapsed" />
                        </Border>

                        <ContentPresenter x:Name="ContentPresenter"
                                          Grid.Column="1"
                                          Margin="8,0,0,0"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          ContentSource="Content"
                                          RecognizesAccessKey="True" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasContent" Value="False">
                            <Setter TargetName="ContentPresenter" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="ContentPresenter" Property="Margin" Value="0" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="CheckBoxGlyphHost" Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckBoxGlyphHost" Property="Background" Value="{DynamicResource SystemAccentColorPrimaryBrush}" />
                            <Setter TargetName="CheckBoxGlyphHost" Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}" />
                            <Setter TargetName="CheckGlyph" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="CheckBoxGlyphHost" Property="Opacity" Value="0.45" />
                            <Setter TargetName="ContentPresenter" Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ui:Button}" BasedOn="{StaticResource {x:Type ui:Button}}">
        <Setter Property="CornerRadius" Value="{StaticResource Win11ControlCornerRadius}" />
        <Setter Property="controls:MotionAssist.IsPressEnabled" Value="True" />
    </Style>

    <Style TargetType="{x:Type vio:MessageBoxDialog}" BasedOn="{StaticResource {x:Type vio:MessageBoxDialog}}">
        <Setter Property="CornerRadius" Value="{StaticResource ContentDialogCornerRadius}" />
    </Style>

    <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
        <Style.Resources>
            <SolidColorBrush x:Key="ControlStrokeColorDefaultBrush" Color="Transparent" />
            <SolidColorBrush x:Key="TextControlFocusedBorderBrush" Color="Transparent" />
        </Style.Resources>
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Effect" Value="{x:Null}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource EmerdeTextInputBorderBrush}" />
        <Setter Property="controls:InputAssist.CommitOnEnter" Value="True" />
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}" />
                <Setter Property="Effect" Value="{x:Null}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type ui:TextBox}" BasedOn="{StaticResource {x:Type ui:TextBox}}">
        <Style.Resources>
            <SolidColorBrush x:Key="ControlStrokeColorDefaultBrush" Color="Transparent" />
            <SolidColorBrush x:Key="TextControlFocusedBorderBrush" Color="Transparent" />
        </Style.Resources>
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Effect" Value="{x:Null}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource EmerdeTextInputBorderBrush}" />
        <Setter Property="controls:InputAssist.CommitOnEnter" Value="True" />
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}" />
                <Setter Property="Effect" Value="{x:Null}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type ui:PasswordBox}" BasedOn="{StaticResource {x:Type ui:PasswordBox}}">
        <Style.Resources>
            <SolidColorBrush x:Key="ControlStrokeColorDefaultBrush" Color="Transparent" />
            <SolidColorBrush x:Key="TextControlFocusedBorderBrush" Color="Transparent" />
        </Style.Resources>
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Effect" Value="{x:Null}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource EmerdeTextInputBorderBrush}" />
        <Setter Property="controls:InputAssist.CommitOnEnter" Value="True" />
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}" />
                <Setter Property="Effect" Value="{x:Null}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource {x:Type ComboBox}}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Effect" Value="{x:Null}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}" />
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}" />
                <Setter Property="Effect" Value="{x:Null}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type ui:CardControl}" BasedOn="{StaticResource {x:Type ui:CardControl}}">
        <Setter Property="CornerRadius" Value="{StaticResource Win11ControlCornerRadius}" />
    </Style>

    <Style TargetType="{x:Type ui:CardExpander}" BasedOn="{StaticResource {x:Type ui:CardExpander}}">
        <Setter Property="Background" Value="{DynamicResource EmerdePanelBrush}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="{StaticResource Win11ControlCornerRadius}" />
        <Setter Property="Padding" Value="14,12" />
        <Setter Property="ContentPadding" Value="0,10,0,12" />
        <Setter Property="ClipToBounds" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ui:CardExpander}">
                            <Border x:Name="RootBorder"
                                    MinHeight="{TemplateBinding MinHeight}"
                                    Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            ClipToBounds="True"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Border x:Name="HeaderChrome"
                                    Grid.Row="0"
                                    Padding="{TemplateBinding Padding}"
                                    Background="Transparent"
                                    CornerRadius="{TemplateBinding CornerRadius}">
                                <Grid MinHeight="54">
                                    <ToggleButton HorizontalAlignment="Stretch"
                                                  VerticalAlignment="Stretch"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  Cursor="Hand"
                                                  Focusable="False"
                                                  IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="{x:Type ToggleButton}">
                                                <Border Background="Transparent" />
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    <ContentPresenter Margin="0,0,36,0"
                                                      VerticalAlignment="Center"
                                                      ContentSource="Header"
                                                      RecognizesAccessKey="True"
                                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                    <Path x:Name="ChevronPath"
                                          Width="9"
                                          Height="6"
                                          HorizontalAlignment="Right"
                                          VerticalAlignment="Center"
                                          Margin="0,0,10,0"
                                          Data="M 0 6 L 4.5 1 L 9 6"
                                          Fill="{DynamicResource TextFillColorSecondaryBrush}"
                                          IsHitTestVisible="False"
                                          RenderTransformOrigin="0.5,0.5"
                                          Stretch="Fill">
                                        <Path.RenderTransform>
                                            <RotateTransform Angle="0" />
                                        </Path.RenderTransform>
                                    </Path>
                                </Grid>
                            </Border>

                            <Border x:Name="ExpandSite"
                                    Grid.Row="1"
                                    MaxHeight="0"
                                    Opacity="0"
                                    Padding="{TemplateBinding ContentPadding}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="0,1,0,0"
                                    ClipToBounds="True"
                                    IsHitTestVisible="False"
                                    RenderTransformOrigin="0.5,0">
                                <Border.RenderTransform>
                                    <TranslateTransform Y="-6" />
                                </Border.RenderTransform>
                                <ContentPresenter ContentSource="Content"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Border>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="True">
                            <Setter TargetName="ExpandSite" Property="MaxHeight" Value="1600" />
                            <Setter TargetName="ExpandSite" Property="Opacity" Value="1" />
                            <Setter TargetName="ExpandSite" Property="IsHitTestVisible" Value="True" />
                            <Setter TargetName="HeaderChrome" Property="CornerRadius" Value="8,8,0,0" />
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ChevronPath"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                         To="180"
                                                         Duration="0:0:0.18">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="ExpandSite"
                                                         Storyboard.TargetProperty="MaxHeight"
                                                         To="1600"
                                                         Duration="0:0:0.22">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="ExpandSite"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="1"
                                                         Duration="0:0:0.16" />
                                        <DoubleAnimation Storyboard.TargetName="ExpandSite"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                                         To="0"
                                                         Duration="0:0:0.18">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ChevronPath"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                         To="0"
                                                         Duration="0:0:0.16">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="ExpandSite"
                                                         Storyboard.TargetProperty="MaxHeight"
                                                         To="0"
                                                         Duration="0:0:0.18">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseIn" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="ExpandSite"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0"
                                                         Duration="0:0:0.12" />
                                        <DoubleAnimation Storyboard.TargetName="ExpandSite"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                                         To="-6"
                                                         Duration="0:0:0.16">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseIn" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HeaderChrome" Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="RootBorder" Property="Opacity" Value="0.55" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ui:NumberBox}" BasedOn="{StaticResource {x:Type ui:NumberBox}}">
        <Style.Resources>
            <SolidColorBrush x:Key="ControlStrokeColorDefaultBrush" Color="Transparent" />
            <SolidColorBrush x:Key="TextControlFocusedBorderBrush" Color="Transparent" />
        </Style.Resources>
        <Setter Property="ClearButtonEnabled" Value="False" />
        <Setter Property="Effect" Value="{x:Null}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource EmerdeTextInputBorderBrush}" />
        <Setter Property="controls:InputAssist.CommitOnEnter" Value="True" />
        <Setter Property="MinHeight" Value="34" />
        <Setter Property="SpinButtonPlacementMode" Value="Compact" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}" />
                <Setter Property="Effect" Value="{x:Null}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type controls:CompactNumberBox}">
        <Setter Property="MinHeight" Value="34" />
        <Setter Property="MinWidth" Value="72" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:CompactNumberBox}">
                    <Border x:Name="RootBorder"
                            MinHeight="{TemplateBinding MinHeight}"
                            Background="{DynamicResource ControlFillColorDefaultBrush}"
                            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource Win11ControlCornerRadius}"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="36" />
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="PART_TextBox"
                                     Grid.Column="0"
                                     MinWidth="0"
                                     Padding="12,0,2,0"
                                     HorizontalContentAlignment="Left"
                                     VerticalContentAlignment="Center"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     controls:InputAssist.CommitOnEnter="False"
                                     FocusVisualStyle="{x:Null}"
                                     Foreground="{TemplateBinding Foreground}"
                                     SnapsToDevicePixels="True">
                                <TextBox.Template>
                                    <ControlTemplate TargetType="{x:Type TextBox}">
                                        <ScrollViewer x:Name="PART_ContentHost"
                                                      Margin="0"
                                                      Background="Transparent"
                                                      Focusable="False"
                                                      HorizontalScrollBarVisibility="Hidden"
                                                      VerticalScrollBarVisibility="Hidden" />
                                    </ControlTemplate>
                                </TextBox.Template>
                            </TextBox>

                            <Grid Grid.Column="1"
                                  Margin="0,1,1,1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <RepeatButton x:Name="PART_IncreaseButton"
                                              Grid.Row="0"
                                              MinWidth="34"
                                              Padding="0"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Focusable="False">
                                    <Path Width="7"
                                          Height="4"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Data="M 0 4 L 3.5 0 L 7 4 Z"
                                          Fill="{DynamicResource TextFillColorSecondaryBrush}"
                                          Stretch="Fill" />
                                </RepeatButton>
                                <RepeatButton x:Name="PART_DecreaseButton"
                                              Grid.Row="1"
                                              MinWidth="34"
                                              Padding="0"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Focusable="False">
                                    <Path Width="7"
                                          Height="4"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Data="M 0 0 L 3.5 4 L 7 0 Z"
                                          Fill="{DynamicResource TextFillColorSecondaryBrush}"
                                          Stretch="Fill" />
                                </RepeatButton>
                            </Grid>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="RootBorder" Property="Opacity" Value="0.55" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
