<vio:ContentDialog x:Class="Emerde.Views.AutoShutdownContentDialog"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                   xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
                   Title="定时关闭"
                   d:DesignHeight="NaN"
                   d:DesignWidth="NaN"
                   FontSize="14"
                   Style="{StaticResource DefaultVioletaContentDialogStyle}"
                   mc:Ignorable="d">
    <ui:StackPanel MinWidth="560" Spacing="18">
        <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Text="{Binding Description}"
                   TextWrapping="Wrap" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"
                    Margin="0,0,8,0"
                    Content="取消关闭"
                    Click="CancelClick" />
            <Button Grid.Column="1"
                    Margin="4,0"
                    Content="现在关闭"
                    Click="ShutdownNowClick" />
            <Button Grid.Column="2"
                    Margin="8,0,0,0"
                    Content="转码完关闭"
                    Click="ShutdownAfterTranscodeClick" />
        </Grid>
        <Button Height="38"
                HorizontalAlignment="Stretch"
                Content="我知道了"
                Click="AcknowledgeClick" />
    </ui:StackPanel>
</vio:ContentDialog>
